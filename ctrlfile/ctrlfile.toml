[toolchain]
compiler = "zig"
optimizer = "ReleaseFast"

[output]
name = "myapp"
path = "bin/myapp"

[tools]
required = ["zig", "git", "curl"]

[installer]
manager = "pacman"
cmd = "sudo pacman -S --noconfirm {tool}"
